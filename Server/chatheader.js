// Register Action.

var action = {
    'Target Action':'Register',
    'UserName':UserName
};

// Send msg to a user

var action = {
    'Target Action':'Send msg',
    'Target User':UserName,
    'Image': //still needed to be handled here.
};